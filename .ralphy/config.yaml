project:
  name: gitlab-cicd-dashboard
  language: TypeScript
  framework: React
  description: ""
commands:
  test: npm test
  lint: npm run lint
  build: npm run build
rules:
  - Follow CLAUDE.md guidelines for commits and branches
  - Run lint, build, and test before marking tasks complete
  - Use existing component patterns from src/components/
  - E2E tests in e2e/ folder use Playwright
boundaries:
  never_touch: null
