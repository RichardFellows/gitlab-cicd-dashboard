name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '16'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm test
        
      - name: Build project
        run: npm run build
        
      - name: Verify build output
        run: |
          echo "Verifying build output in public directory..."
          ls -la public/
          echo "Checking for critical JS files..."
          if [ ! -f public/gitlab-api-service.js ] || [ ! -f public/dashboard-data-service.js ] || [ ! -f public/pipeline-performance.js ]; then
            echo "Error: Required JS files not found in public directory!"
            exit 1
          fi
          echo "Build verification completed successfully."
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload the public directory
          path: './public'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
  
  post-deployment-test:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '16'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Wait for deployment
        run: sleep 60  # Wait to make sure the deployment is fully propagated
      
      - name: Run post-deployment tests
        run: npm run test:deployment ${{ needs.deploy.outputs.page_url || 'https://richardfellows.github.io/gitlab-cicd-dashboard/' }}
