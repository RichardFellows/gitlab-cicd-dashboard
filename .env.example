# GitLab CI/CD Dashboard — Environment Configuration
# Copy to .env and fill in your values.
# All VITE_ prefixed vars are available at runtime in the browser.

# ─── GitLab Instance ───────────────────────────────────────────
# Base URL for the GitLab API (without trailing slash)
VITE_GITLAB_URL=https://gitlab.com/api/v4

# Personal Access Token (read_api scope minimum)
# Leave blank to require manual entry in the UI
VITE_GITLAB_TOKEN=

# ─── Default Dashboard Settings ───────────────────────────────
# Default timeframe in days for pipeline history
VITE_DEFAULT_TIMEFRAME=30

# Comma-separated GitLab group IDs to load by default
VITE_DEFAULT_GROUPS=

# Comma-separated GitLab project IDs to load by default
VITE_DEFAULT_PROJECTS=

# ─── Optional Integrations ────────────────────────────────────
# JIRA base URL for branch→ticket linking (e.g. https://jira.company.com/browse)
VITE_JIRA_BASE_URL=

# ─── Build / Deploy (not exposed to browser) ──────────────────
# GitLab PAT for pushing (CI only)
GITLAB_PAT=

# Cloudflare Pages deployment
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_PROJECT_NAME=
